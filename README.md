#  Tiktrack system.

run: 
"composer install"
"npm install"
"php artisan migrate"
"php artisan serve"
"npm run dev" or plain: "dev"

Change column admin boolean to 1 at user table when user is admin.

Have fun working 👌
